import { typeIcons } from "./typeIcons"
import { enumSize } from "./enumSizes"

export interface ICircles{ 
  size: string,
  color: string
  iconName?: typeIcons,
  iconSize?: string,
  iconColor: string
}